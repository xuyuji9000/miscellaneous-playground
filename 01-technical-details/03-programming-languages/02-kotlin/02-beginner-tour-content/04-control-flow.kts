// 1. If 

println("--- If ---")

var d: Int

val check = true

if (check) {
    d = 1
} else {
    d = 2
}

println(d)

// use if as an expression
val a = 1
val b = 2

println(if (a>b) a else b)

// 2. When

println("--- When ---")
